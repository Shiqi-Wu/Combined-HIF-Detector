# Config Layout

- `lstm/`: configs for the BiLSTM baseline
- `dynamic/`: configs for dynamic-system experiments
- `koopman/`: configs for Koopman and residual Koopman methods
- `runtime/`: runtime or launcher configs such as Accelerate
