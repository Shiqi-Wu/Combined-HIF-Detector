# `src/common`

Shared utilities used by multiple method families.

- `dataloader.py`: dataset loading, windowing, scaling, and PCA preprocessing
- `args.py`: shared CLI argument helpers
- `loss.py`: shared loss helpers

If you need to change preprocessing, start with `dataloader.py`.
