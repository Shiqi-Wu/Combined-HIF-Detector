# `src/dynamic`

Physics-informed dynamic-system baseline.

- `model.py`: estimation of shared `K` and class-specific `B(p)`
- `eval.py`: known-control evaluation pipeline

This method is currently stable only in the known-control setting.
