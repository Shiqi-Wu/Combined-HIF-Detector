# `src/lstm`

BiLSTM baseline for HIF localization.

- `model.py`: BiLSTM classifier definition
- `train.py`: training entrypoint
- `eval.py`: standalone evaluation entrypoint

Primary config:

- `configs/lstm/classifier.json`
